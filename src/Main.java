public class Main {
    public static void main(String[] args) {
    double springCost = 10000;
    double summerCost = 8000;
    double fallCost = 6000;
    double winterCost = 12000;
    double annualCost = springCost + summerCost + fallCost + winterCost;
    System.out.println("The annual cost is " + springCost + " for spring, " + summerCost + " for summer, " + fallCost + " for fall, and " + winterCost + " for winter, totaling " + annualCost);
    }
}
